# US-006 Slice-01 / S10 (CP-6) — Verification Report

**Date:** 2026-06-08
**Task type:** AD-HOC (plan step S10; no `tasks.json` row exists in this checkout — tracked in `robot/docs/stories/US-006-learning-course-runtime/slice-01-vertical-implementation-plan.md` §13)
**Owning repo:** tbot-firmware (`robot/TBOT-Firmware`)
**Lane:** LANE-FIRMWARE (parallel to LANE-BACKEND; depends only on S2/CP-2 frozen protocol + ADR 0013)

## Task Summary
Implemented the additive `lesson_*` renderer in the main firmware: a null-guard + an additive dispatch branch in `OnIncomingJson`, a self-contained `Application::HandleLessonMessage` (sub-dispatching `lesson_prepare/start/step/stop`), the `hello.features` lesson-capability advertisement, an additive public `Protocol::SendLessonFrame`, and CMake registration. The handler renders one espTft `lesson_step` (s4 "model") onto the real LVGL display via the degraded fallback ladder and emits the canonical `lesson_ack` / `lesson_progress` / `lesson_error` frames. The slice is strictly additive: the 8 legacy message types, the Google-Live voice path, and the MCP arm tools are unchanged. Author deliverable only — CP-6 is a separate blocking reviewer pass.

## Files Changed
- `main/lesson_handler.cc` (new, 339 lines) — `HandleLessonMessage`: parse + null-guard envelope, exact-string `protocolVersion` + `espTft` gate, staleness/dedup, render ladder, canonical ack/progress/error emit.
- `main/lesson_handler.h` (new, 43 lines) — frozen contract constants (`kLessonProtocolVersion`/`kLessonRendererName`/`kLessonProfileEspTft`) + the D-PRELOAD-OWNER byte-source decision note.
- `main/application.cc` (+14, 0 existing lines altered) — null-guard immediately after the `type` fetch; additive `else if (strncmp(type->valuestring,"lesson_",7)==0)` branch immediately above the unchanged unknown-type no-op.
- `main/application.h` (+5) — `void HandleLessonMessage(const cJSON*)` declaration.
- `main/protocols/protocol.h` (+5) — additive public `bool SendLessonFrame(const std::string&)`.
- `main/protocols/protocol.cc` (+6) — `SendLessonFrame` definition (delegates to the existing protected `SendText`).
- `main/protocols/websocket_protocol.cc` (+7) — `#include "lesson_handler.h"`; additive `features.lesson=true` + `features.renderer`.
- `main/CMakeLists.txt` (+1) — registered `lesson_handler.cc` in `SOURCES`.
- `tests/test_lesson_dispatch_backward_compat.py` (new, 116 lines, 12 test functions) — CP-6 backward-compat + canonical-ack static proof.

## Verification Matrix

| Task | AC# | Docs Claim | Code Reality | Test Evidence | Runtime Evidence | Verdict |
|------|-----|------------|-------------|---------------|------------------|--------|
| US-006/S10 | AC1 | ESP-IDF build for `lcdwiki-es3c35p` is GREEN (plan §13 CP-6) | whole slice compiles; `lesson_handler.cc.obj` built + linked | `pytest … test_lesson_handler_registered_in_build` PASS | `idf.py -B build-es3c35p build` → "Project build complete"; `xiaozhi.bin` 3,366,016 B (0x335820), 19% partition free; no errors/warnings | PASS |
| US-006/S10 | AC2 | 8 legacy types + voice + MCP arm tools byte-for-byte unchanged; unknown-type no-op still fires | 8 `strcmp` branches intact & ordered (`application.cc:1352–1466`); no-op intact (`:1486`); `mcp_server.cc` zero lesson refs (10 tools) | `test_eight_legacy_dispatch_branches_present_and_ordered`, `test_unknown_type_noop_is_unchanged`, `test_handle_robot_action_message_untouched_and_mcp_arm_tools_have_no_lesson_leak` PASS; 12/12 voice tests PASS | build GREEN (voice/MCP compile unchanged) | PASS |
| US-006/S10 | AC3 | Null-guard `if (!cJSON_IsString(type)) {…return;}` immediately after the `type` fetch | `application.cc:1342` fetch → `:1348` guard → `:1352` first branch | `test_null_guard_added_immediately_after_type_fetch_and_before_first_branch` PASS | build GREEN | PASS |
| US-006/S10 | AC4 | Additive `lesson_` branch immediately above the unknown-type no-op; no-op unmoved | `application.cc:1480` branch → `:1484` `HandleLessonMessage` → `:1486` no-op | `test_lesson_branch_is_additive_above_the_unknown_type_noop` PASS | build GREEN | PASS |
| US-006/S10 | AC5 | `HandleLessonMessage` sub-dispatches prepare/start/step/stop; renders s4 via the ladder (poster→PNG→glyph→emoji+caption); Layer-3 = emoji-face v1; marshalled like the TTS pattern | `lesson_handler.cc` dispatch + ladder + `Schedule([display,…]{SetEmotion/SetChatMessage})` | covered by ack/gate/no-download tests (below) | **render pixel-leg HARDWARE-GATED (plan §10.5) — manual bench on physical `lcdwiki-es3c35p`; not CI-provable.** Always-available floor (emoji-face + caption + glyph) compiles & draws via base `Display` API | PASS (build) / UNVERIFIABLE-IN-CI (pixel render — hardware-gated by design) |
| US-006/S10 | AC6 | `hello.features {lesson:true, renderer:"teebot-lesson-renderer.v1"}` (D-CAP-FLAG) | `websocket_protocol.cc:274–275` | `test_hello_features_advertises_lesson_capability_additively` PASS | build GREEN | PASS |
| US-006/S10 | AC7 | Canonical `lesson_ack`: `body.acks` echoes acked seq; envelope `sequence` = firmware F→S counter; NO `ackFor`; `lessonVersion` numeric; null-guard every envelope + `body.scene.*` | `lesson_handler.cc` `emit_ack`/`BuildFrame`/`CopyNum`; F→S 1‑2‑3‑4‑5 matches fixture | `test_lesson_ack_shape_is_canonical_body_acks_no_ackfor`, `test_lessonversion_is_echoed_as_a_number_not_a_string` PASS | build GREEN | PASS |
| US-006/S10 | AC8 | Gate render on `protocolVersion` exact-string `== "teebot-lesson-renderer.v1"` AND `body.profile=="espTft"` (else `lesson_error LESSON_VERSION_UNSUPPORTED`, render nothing) | `lesson_handler.cc` gate block; `lesson_handler.h` constants | `test_render_is_gated_on_exact_contract_identity_and_esptft_profile` PASS | build GREEN | PASS |
| US-006/S10 | AC9 | Firmware does NOT download/checksum (D-PRELOAD-OWNER); no Servant change; registered in CMakeLists | `AssetAvailable` probes on-device only; Servant untouched; `CMakeLists.txt:37` | `test_firmware_does_not_download_or_checksum_assets`, `test_lesson_handler_registered_in_build` PASS | build GREEN | PASS |
| US-006/S10 | DoD | CP-6 (BACKWARD-COMPAT PROOF): build green; static-prove additive guarantee + canonical ack | all of the above | 24/24 pytest PASS (12 backward-compat + 12 voice non-regression) | build GREEN | PASS (author scope; CP-6 sign-off = separate reviewer pass) |

## Validation Checklist Results
No `.agent/VALIDATION_CHECKLIST.md` exists in this checkout (firmware repo has no `.agent/`). The mechanical gate is satisfied by: ESP-IDF build GREEN (compile+link+image) and 24/24 static contract tests. No `// TODO`/`// FIXME` added in changed code; no swallowed errors; no test weakened to mask a defect (the one refined assertion tightened `ackFor`→`"ackFor"` to match the JSON-key form, the code was always correct); no `any`/suppressions; no `--no-verify`.

## Critique-Before-Close Answers (summary)
- **Q1 (root vs symptom):** Root — additive dispatch + handler at the real anchors; the unsolvable-in-slice piece (image bytes) is surfaced, not faked. Not a symptom patch.
- **Q2 (code vs docs):** Matches the frozen fixture (envelope/ack/scene), ADR 0013 §C/§I/§J/§M. One documented render-dependent divergence (`degraded` value).
- **Q3 (tests meaningful):** Each static test fails on a real regression; voice tests stay green. The refined `ackFor` assertion is precision, not gaming.
- **Q4 (drift):** No new drift introduced; the pre-existing plan-vs-code byte-source contradiction was surfaced (not silently reconciled).
- **Q5 (principal accept):** Yes, with the surfaced boundary noted (poster/PNG = hardware-gated leg; scoring deferred per §7.4.2; ESP arbitrates sequencing).
- **Q6 (reproducible):** Yes — commands below run from committed/on-disk files.

## Drift-Check Result
CLEAR — no code-vs-doc drift introduced. Pre-existing plan-vs-code contradictions **surfaced** (not new): (C1) "render poster + barn PNG" has no in-slice byte source (D-PRELOAD-OWNER + assets not flashed + no ESP→firmware byte channel) → resolved per plan §10.5 (render leg hardware-gated) + §7.4 (image methods deferred) + DIV-FW-DECODE; (C2) the "null-deref today" premise is already mitigated upstream (both transports pre-guard `cJSON_IsString(type)`) — guard added anyway as mandated defense-in-depth.

## Verdict
**DONE** (CP-6 author scope) — the additive contract, backward-compat guarantee, canonical ack, and GREEN build are complete and statically proven; the poster/PNG pixel render is a prominently surfaced, plan-anticipated hardware-gated boundary (§10.5), not a hidden gap. CP-6 sign-off remains a separate blocking reviewer checkpoint.

## Reproducibility
To replay this verification, run in `robot/TBOT-Firmware`:
```bash
# 1) Static contract proof (no hardware/IDF needed) — expect: 24 passed
python3 -m pytest tests/test_lesson_dispatch_backward_compat.py tests/test_realtime_voice_state.py -q

# 2) ESP-IDF build for the espTft board — expect: "Project build complete" + build-es3c35p/xiaozhi.bin
. /Users/manhhodinh/esp/esp-idf-v5.5.2/export.sh   # any ESP-IDF v5.5.x export.sh
idf.py -B build-es3c35p build

# 3) Manual (hardware-gated, not CI): flash one internal lcdwiki-es3c35p, send the
#    fixture lesson_step s4, observe emoji-face + caption render + lesson_ack(body.acks=3).
```
